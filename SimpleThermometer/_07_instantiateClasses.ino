MovingAverage ps0_filter(0.0); // Not the same as tempC_filtered, which will be the final filtered output
MovingAverage ps1_filter(0.0); // Not the same as tempC_filtered, which will be the final filtered output
